
import React from 'react';
import type { PlantRecommendation } from '../types';
import SunIcon from './icons/SunIcon';
import WaterIcon from './icons/WaterIcon';
import TipIcon from './icons/TipIcon';


interface RecommendationProps {
  recommendation: PlantRecommendation;
  onRestart: () => void;
}

const CareInstructionCard: React.FC<{
  icon: React.ReactNode;
  title: string;
  text: string;
}> = ({ icon, title, text }) => (
  <div className="flex items-start p-4 bg-green-50 rounded-lg">
    <div className="flex-shrink-0 text-green-600">{icon}</div>
    <div className="ml-4">
      <h4 className="font-bold text-gray-800">{title}</h4>
      <p className="text-gray-600 text-sm">{text}</p>
    </div>
  </div>
);

const Recommendation: React.FC<RecommendationProps> = ({ recommendation, onRestart }) => {
  return (
    <div className="w-full max-w-2xl mx-auto p-4 md:p-8 bg-white rounded-2xl shadow-lg animate-fade-in">
        <div className="text-center mb-6">
            <h2 className="text-xl text-green-700 font-semibold">당신을 위한 반려식물은...</h2>
            <h1 className="text-4xl md:text-5xl font-extrabold text-gray-800 mt-2">{recommendation.plantName}</h1>
        </div>

        <div className="mb-6">
            {recommendation.imageUrl ? (
                <img 
                    src={recommendation.imageUrl} 
                    alt={recommendation.plantName} 
                    className="w-full h-64 md:h-80 object-cover rounded-xl shadow-md" 
                    referrerPolicy="no-referrer"
                />
            ) : (
                <div className="w-full h-64 md:h-80 bg-gray-200 rounded-xl flex items-center justify-center">
                    <span className="text-gray-500">이미지 준비 중</span>
                </div>
            )}
        </div>
        
        <div className="bg-amber-50/50 p-6 rounded-xl mb-6">
            <h3 className="text-lg font-bold text-gray-800 mb-2">🌱 추천 이유</h3>
            <p className="text-gray-700 leading-relaxed">{recommendation.reason}</p>
        </div>

        <div className="mb-8">
            <h3 className="text-lg font-bold text-gray-800 mb-4">🌿 관리 방법</h3>
            <div className="space-y-3">
                <CareInstructionCard 
                    icon={<WaterIcon className="w-6 h-6" />}
                    title="물주기"
                    text={recommendation.careInstructions.water}
                />
                <CareInstructionCard 
                    icon={<SunIcon className="w-6 h-6" />}
                    title="햇빛"
                    text={recommendation.careInstructions.light}
                />
                <CareInstructionCard 
                    icon={<TipIcon className="w-6 h-6" />}
                    title="관리 팁"
                    text={recommendation.careInstructions.tip}
                />
            </div>
        </div>

        <div className="text-center">
            <button
                onClick={onRestart}
                className="bg-green-600 text-white font-bold py-3 px-8 rounded-full hover:bg-green-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-green-500 transition-all duration-300 transform hover:scale-105"
            >
                다시하기
            </button>
        </div>
    </div>
  );
};

export default Recommendation;
